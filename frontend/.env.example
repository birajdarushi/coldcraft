# Backend API base URL (no trailing slash).
# Leave empty for local dev — Vite proxies /api and /track to localhost:8000.
# On Vercel, set this in Project → Settings → Environment Variables.
VITE_API_URL=

# Optional dev links in the shell header (defaults to localhost when unset).
VITE_MAILPIT_URL=http://localhost:8025