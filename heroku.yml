# Heroku container deploy for the Coldcraft FastAPI backend.
# Set the app to the container stack once:  heroku stack:set container -a <app>
# Then deploy:  git push heroku <branch>:main
build:
  docker:
    web: Dockerfile
run:
  web: uvicorn coldcraft.api.app:app --host 0.0.0.0 --port $PORT
